# default path
PATH=/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin

# Add homebrew sbin folder to the `$PATH`
PATH=/usr/local/sbin:$PATH

# Add `~/bin` to the `$PATH`
PATH=$HOME/bin:$PATH

# If there is bin folder in the current location, it takes preference
# Usefull for binstubs in rails
PATH=./bin:$PATH

# brew
PATH=$(brew --prefix)/bin:$(brew --prefix coreutils)/libexec/gnubin:$PATH

# node 
[[ `which npm` ]] && export PATH=$PATH:$(npm config get prefix)/bin

# Mysql
PATH=$PATH:/usr/local/mysql/bin

# rbenv
PATH=~/.rbenv/bin:$PATH
PATH=./bin:$PATH: